# days-without-failure

Days without failure

## config.yaml

```yml
---
address: "localhost"
port: 8080
basepath: "/"
debug: false
locale: "en"
timezone: "UTC"
endpoints:
  - path: "j"
    url: "http://localhost/jenkins"
    type: "JENKINS"
```
